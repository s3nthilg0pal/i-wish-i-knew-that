# Chrome Extension

This extension saves the current tab to the link API.

## Load locally

1. Open `chrome://extensions`.
2. Enable **Developer mode**.
3. Click **Load unpacked**.
4. Select this `extension` folder.

The popup posts to `https://api.senthil.nz/links` by default. Open **Options** in the popup to change the API URL.
